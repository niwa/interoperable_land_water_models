## Windows DLL wrapping the Sparrow Router

You can run this in bmi-runner from a command line. CD to the model directory and launch the program like this:

  > bmi-runner ..\wrapper\lib\bmi_sparrow .\Config.yaml
  
  
